<!DOCTYPE html>
<html>
  <head>
    <title>Cooksathi</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <header class="bg-green-600 text-white py-4">
      <div class="container mx-auto px-4">
        <div class="flex justify-between items-center">
          <h1 class="text-2xl font-bold">
            <%= link_to "Cooksathi", root_path, class: "hover:text-green-100" %>
          </h1>
          <nav class="space-x-4">
            <%= link_to "Home", root_path, class: "hover:text-green-100" %>
            <%= link_to "Browse Recipes", recipes_path, class: "hover:text-green-100" %>
            <% if logged_in? %>
              <%= link_to "My Favorites", favorites_path, class: "hover:text-green-100" %>
              <%= link_to "Create Recipe", new_recipe_path, class: "hover:text-green-100" %>
              <span class="text-green-100">|</span>
              <span class="text-green-100"><%= current_user.name %></span>
              <%= button_to "Logout", logout_path, method: :delete, class: "inline-block hover:text-green-100" %>
            <% else %>
              <%= link_to "Login", login_path, class: "hover:text-green-100" %>
              <%= link_to "Sign Up", signup_path, class: "hover:text-green-100" %>
            <% end %>
          </nav>
        </div>
      </div>
    </header>

    <main class="container mx-auto px-4 py-8">
      <% if flash[:notice] %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
          <%= flash[:alert] %>
        </div>
      <% end %>
      <%= yield %>
    </main>

    <footer class="bg-gray-800 text-white py-6 mt-12">
      <div class="container mx-auto px-4 text-center">
        <p>Cooksathi - Your kitchen companion</p>
      </div>
    </footer>
  </body>
</html>
